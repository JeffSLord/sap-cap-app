ID: cap_demo
_schema-version: "2.1"
version: 0.0.1
modules:
  - name: cap_demo-db
    type: hdb
    path: db
    parameters:
      memory: 256M
      disk-quota: 512M
    requires:
      - name: cap_demo-db-hdi-container
  - name: cap_demo-srv
    type: nodejs
    path: srv
    parameters:
      memory: 512M
      disk-quota: 512M
    provides:
      - name: srv_api
        properties:
          url: ${default-url}
    requires:
      - name: cap_demo-db-hdi-container
resources:
  - name: cap_demo-db-hdi-container
    type: com.sap.xs.hdi-container
    properties:
      hdi-container-name: ${service-name}
